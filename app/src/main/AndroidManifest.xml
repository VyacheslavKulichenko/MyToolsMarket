<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kulichenko.mytoolsmarket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyToolsMarket">
        <activity android:name=".ShlifmashDetailActivity"></activity>
        <activity android:name=".FenCategoryActivity" />
        <activity android:name=".RubanCategoryActivity" />
        <activity android:name=".ShlifmashCategoryActivity" />
        <activity android:name=".PilCategoryActivity" />
        <activity android:name=".BolgarCategoryActivity" />
        <activity android:name=".PerfCategoryActivity" />
        <activity android:name=".DrillCategoryActivity" />
        <activity android:name=".InstrumDetailActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>